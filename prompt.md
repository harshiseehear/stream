record details field names should be left aligned

remove the bounds by the window dimensions 
graph should be free to goout of bounds
introduce zoom in and our with scroll
enable drag and move across canvas if use clicks on the canvas and drags

template node needs to eb a circle just like the records just bigger in radius,
remove the current styling of the template node
